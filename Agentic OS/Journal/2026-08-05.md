---
tags: [journal, agentic-os, 2026-08-05]
date: 2026-08-05
---

# 📓 Journal — 2026-08-05

### 00:10
**VTO build is GREEN for the first time.** Consolidated vault context into Claude memory ([[vto-project]], [[vto-build-state]]), then completed the half-applied D3 build so `vto-core` compiles and the widget builds. Committed as `ffa686f` (not pushed); queued the remaining tasks; explained how to add the trained model.

### 02:30
**Real BiSeNet segmentation model integrated (T019b done).** Rohit delivered a verified `bisenet_glasses.onnx` (12.6 MB int8, 19-class CelebAMask-HQ, class 6 = glasses). Replaced the GlassesSegmenter stub with **real ONNX inference** (decode→32-mult grid→ImageNet normalize→NCHW→run→argmax class-6→upsample→GlassesMask), mirroring the LamaInpainter ORT pattern; multi-threaded WASM; injectable test seam. Wired `create()` into `ensureRemovalPipeline()` behind `?vtoModel=` — **non-blocking + fallback-safe** (failure → contour FrameDetector, try-on never breaks). Model hosted as an extension asset (gitignored). Interim class-6→`frame`; the 3-class frame/lens (F003) slots in behind the same API (flagged in code). **Verified green:** tsc 0, eslint 0, 278/278 tests, widget builds (1 shell chunk). Browser end-to-end + mask consumption (T021) are the next steps. Uncommitted — awaiting Rohit's git review.

### 15:40
**Model hosted on Shopify CDN.** The `.onnx` can't live in extension assets (Shopify theme extensions only allow jpg/json/js/css/png/svg/wasm — `shopify app deploy` failed on it). Removed it from assets (unblocked the deploy), then uploaded the model to the **ankurs-vto Shopify Files** via the Admin API (stagedUploadsCreate → GCS POST 201 → fileCreate GenericFile, renamed `.wasm` for the type allowlist; ORT loads by bytes). Live + verified: `https://cdn.shopify.com/s/files/1/0845/5936/4316/files/bisenet_glasses.wasm?v=1785925549` (200, CORS `*`, valid ONNX header, 12.64 MB). Activate with `?vtoModel=<url>`.

### 18:20
**T021 slice 1 — learned mask now drives the cover.** Wired the segmenter's mask into the head-cover REGION path (correct integration point: the interim 19-class model marks the whole glasses region with no frame/lens split, so it drives the cover gate, not the rim-erase which would erase the eyes). Added a pure tested adapter `glassesMaskToRegionSource`; the segmenter runs on the SAME ROI pixels + pass as the contour mask (exact registration) and unions into `coverRegionMask`. **Gated `?vtoSegMask=on`, default OFF** — working cover untouched; safe on-device A/B. Green: tsc 0, eslint 0, **281/281 tests**, widget builds. To see it: `?vtoModel=<url>&vtoSegMask=on`. Multi-pose calibration + on-device tuning split to **T021b**. Uncommitted — awaiting Rohit's git review.

### 19:05
**Deployed `vto-phase1-64`** (`shopify app deploy --config vto-phase1 --allow-updates`, non-interactive via CI=1; `--force` doesn't exist in this CLI). New extension version live to users — the BiSeNet segmenter + T021 cover integration are now on the store. Theme check passed (the .onnx-in-assets error is gone). Test on the try-on page with `?vtoModel=<cdn-url>` (+`&vtoSegMask=on` for the A/B); confirm the `[vto] build …` stamp in console shows the new bundle. Working tree still uncommitted.

### 20:30
**Segmenter diagnostic shipped (`vto-phase1-66`).** Root cause of "can't verify": with only `?vtoModel` set, `segment()` was never called (it was gated on `?vtoSegMask`), so the model loaded but never ran. Fix: the segmenter now runs a throttled (~2 Hz) inference whenever loaded, independent of the cover flag, and logs `[vto] seg: N glasses px (X% ROI) Yms` — rises with glasses on, ~0 off = definitive verification. One inference per pass feeds both the counter and the T021 union. Also fixed the stale "pending T021" ready-log string. Green (281 tests). Note: the periodic diagnostic inference adds a small hitch — the non-blocking (cache-the-mask) fix is the follow-up. Uncommitted.

### 21:15
**Real bug found on-device + fixed (`vto-phase1-67`).** The diagnostic worked and surfaced the actual failure: the int8 QDQ model throws on onnxruntime-web's **WebGPU** backend — "DequantizeLinear: scale and zero-point must have the same rank" (WebGPU's quantized-op support is incomplete). My code preferred webgpu, so inference failed every pass and fell back to empty. Fix: **run the segmenter on the WASM execution provider** (correct for quantized models; `executionProviders` option, default wasm). Green (281 tests), deployed. Now `[vto] seg:` should show real non-zero glasses-pixel counts.

### 22:00
**#2 non-blocking segmenter shipped (`vto-phase1-68`).** The segmenter inference now runs **fire-and-forget** (never awaited inside the removal pass), single-flight via `segRunning`, caching its latest mask; the pass consumes the cache instead of blocking ~60–90 ms on WASM. Cover union uses the cached mask when ROI dims match (region accumulator tolerates the ≤1-frame lag). Cache cleared on try-on start. Restores detection cadence while keeping the segmenter + counter working. Green (281 tests). Still needs the dev-preview/502 resolved to view (that's deployment, not code). Uncommitted.

### 23:30
**Segmenter is now the PRIMARY detector (`vto-phase1-69`).** Traced why nothing was visible: removal was ENTIRELY gated on the weak contour detector firing `hasGlasses`→`arm()`; the BiSeNet segmenter only drove a cosmetic cover region, so it never affected detection — exactly the "additive not primary" mistake. Rohit's directive + new memory rule [[primary-not-additive]]. Fix: `FrameRemovalPipeline.process` now takes the segmenter mask and uses it as the primary detect/mask/removal source (contour = fallback). Interim whole-glasses mask → frame BAND via erosion (eroded interior = lens-keep) so clear-lens eyes survive. Retired `?vtoSegMask`. Green (281 tests), deployed. Rim thickness + threshold need on-device tuning; 3-class model perfects it. Now removal should actually FIRE from the learned detector.
