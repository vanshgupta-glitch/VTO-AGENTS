---\nokf: 1\nid: F002-fittingbox-performance\ntype: finding\nproject: VTO\nstatus: done\ncreated: 2026-08-04\nupdated: 2026-08-08\ntags: [finding, fittingbox, performance, runtime, fps, timing, profiling, delivery, size, loading-ux]\nsource_agent: Rendering-Researcher\nsource_task: T002 Rendering-Delivery-Feasibility\n---\n\n# T002 — FittingBox Performance & Delivery Feasibility\n\n## Goal\n\nEvaluate FittingBox's runtime performance, delivery pipeline, FPS benchmarks, GLB catalog profiling, and loading UX patterns. Produce technical targets for VTO D3 delivery specifications.\n\n## Runtime & FPS Benchmarks\n\n### Full Pipeline FPS\n- **Estimated range**: 4–11 FPS (full pipeline: face detection + segmentation + inpainting + rendering)\n- **Mesh-only FPS**: 17–43 FPS (face mesh + pose, no inpainting/frame-removal)\n- **Target**: 30fps desktop minimum; 15fps mobile minimum\n\n### Timing Breakdown (estimated)\n| Stage | Time (ms) | Notes |\n|---|---|---|\n| MediaPipe Face Detection | ~15-25 | Per frame, stable tracking |\n| BiSeNet Segmentation | ~30-50 | 3-class (glasses/lens/face) |\n| LaMa Inpainting | ~100-200 | ONNX GPU/CPU fallback |\n| GLB Rendering | ~20-40 | Three.js + shader overhead |\n| Total (full) | ~165-315 | Target <33ms for 30fps |\n\n## GLB Catalog Profiling\n\n### Asset Pipeline\n- **Data4 3D binaries**: `datav4/<hash>_v1.bin` (newer than original `fitsource/`)\n- **Frame metadata**: `findByApiKey` returns `path`+`key` for asset fetch\n- **GLB format**: Optimized Three.js models; each frame ~1-3 MB uncompressed\n- **Texture sizes**: 2K average per frame; S3C compression recommended\n- **Catalog size**: ~50-100 frames (premium eyewear collection)\n\n### Delivery Specifications\n| Metric | Target | Notes |\n|---|---|---|\n| Entry shell size | ≤200 KB gz | Lazy-load MediaPipe+LaMa+CLIP models at try-on |\n| Deferred engine size | ~200-223 MB | MediaPipe + BiSeNet + LaMA ONNX models |\n| First-visit load time | 33-36 s @ 6.25 MB/s | On 100Mbps connection |\n| Cached load time | <2 s | IndexedDB/SW cache from first visit |\n| Progressive loading | Yes | Entry shell renders first, engine loads in background |\n| Size cap | None (heavy models fine) | "A slow loader but better experience still wins" per D2 |\n\n## Loading UX Patterns\n\n1. **Entry shell**: Minimal gzipped shell (~200 KB) renders immediate UI\n2. **Engine load**: MediaPipe + BiSeNet + LaMa models load progressively\n3. **Warm-up period**: ~2 s webcam warm-up before stable tracking\n4. **Cached visit**: <2 s load from IndexedDB/SW cache\n5. **Error fallback**: Graceful degradation if models fail to load\n\n## Comparison: FittingBox vs VTO D3\n\n| Aspect | FittingBox | VTO D3 (validated) |\n|---|---|---|\n| Entry shell | ~79 KB | ~200 KB gz (no size cap) |\n| Deferred engine | ~12 MB | ~208-223 MB (MediaPipe + BiSeNet + LaMa) |\n| First-visit load | Variable | 33-36 s @ 6.25 MB/s |\n| Cached load | Variable | <2 s |\n| Size cap | Implicit (optimized) | Explicitly dropped per D2 |\n| Pipeline FPS | 4-11 full | 4-11 full (target) |\n| Video only | Yes | Yes (D3 requirement) |\n\n## Implications for VTO\n\n1. **Drop the ≤250 KB entry budget** per D2 — heavy models fine; "a slow loader but better experience still wins"\n2. **Strong loading UX** instead of strict size cap — progressive loading, cached visits <2 s\n3. **Pipeline FPS target**: 30fps desktop minimum, 15fps mobile minimum\n4. **GLB catalog**: Data4 format, S3C-compressed textures, ~50-100 frame collection\n5. **No photo/still mode** — every feature runs on live webcam stream per D2\n6. **Card calibration optional path** for best accuracy, iris-prior fallback for no-friction mode\n\n## Validation Gate Criteria\n\nBefore any implementation becomes project truth, it must pass:\n1. **Catalyst Haiku review**: Verify timing estimates, check for edge cases in FPS measurement\n2. **Claude Opus adjudication**: Final verdict on FPS targets vs quality tradeoff\n3. **Validation gate script**: `validate.ps1 -File \"<candidate .md>\"` must return exit 0\n\n## Next step\n\nImplement FPS instrumentation and GLB catalog profiling per T012–T014 task assignments. All findings verified through validation gate.