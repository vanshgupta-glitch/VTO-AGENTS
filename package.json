{
  "name": "vto-swarm",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "cli:build": "pnpm --filter vto-swarmctl build",
    "cli:dev": "pnpm --filter vto-swarmctl dev",
    "check": "pnpm cli:check",
    "bootstrap": "pnpm cli:dev bootstrap create-channels"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.12.0"
}